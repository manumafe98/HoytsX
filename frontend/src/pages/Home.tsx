import { HomeMovieSection } from "@/components/HomeMovieSection";
import { Layout } from "@/components/Layout";

export const Home = () => {
  return (
    <Layout>
      <HomeMovieSection />
    </Layout>
  );
};
